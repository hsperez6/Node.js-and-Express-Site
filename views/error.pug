extends layout


block content
	article.portfolio-about
		.inner-wrapper.flex-row-wrap.two-col
			h1= err.message
			h2 Error status: #{err.status}
			pre= err.stack
			nav.nav
				a(href='/')
					span &larr;
					|	Back
